logging:
  level:
    com.pulse.api: debug

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true

# Keep local-friendly defaults here if desired
server:
  compression:
    enabled: false
